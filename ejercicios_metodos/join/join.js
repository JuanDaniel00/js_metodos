// join( )
// Este método devuelve una nueva cadena concatenando todos los elementos de la matriz separados 
// por el separador especificado.

// Pepita necesita usar el método join( ) para separar los elementos de la matriz materiales con "-"

const materiales = ["Madera", "Palos", "Piedras", "Arena", "Tierra", "Agua", "Fuego", "Metal", "Diamante"];

console.log(materiales.join("-"));

// Pepita necesita usar el método join( ) para separar los elementos de la matriz materiales con " "

console.log(materiales.join(" "));

// Pepita necesita usar el método join( ) para añadir un punto al final de la cadena

console.log(materiales.join(".") + ".");